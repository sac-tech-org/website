import type { Event } from "./types";

/**
 * TODO: Populate this schedule from a verified SacTech source.
 *
 * This is intentionally empty. The events page previously contained imported
 * Playful Programming fixtures, which are not SacTech programming and must not
 * be presented as such.
 */
export const events: Event[] = [];
