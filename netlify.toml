[build]
command = "pnpm exec playwright install chromium && pnpm verify"
publish = ".next"

[build.environment]
NODE_VERSION = "24.19.0"

[dev]
command = "pnpm dev:next"
targetPort = 3000
port = 8888
