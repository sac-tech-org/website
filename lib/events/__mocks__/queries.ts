import { fn } from "storybook/test";

export const getApprovedEvents = fn(async () => []);
export const getManagedEventForEdit = fn(async () => null);
export const getPendingEventEdits = fn(async () => []);
export const getPendingEvents = fn(async () => []);
export const getSubmissionsForUser = fn(async () => []);
